import { Logo } from "@/components/Logo";

export const NavbarLoggedin = () => {
  return (
    <nav className={"bg-neutral-850 bg border-b border-neutral-800 px-4 py-2"}>
      <Logo />
    </nav>
  );
};
